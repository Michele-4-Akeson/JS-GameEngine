export default class Words{
    constructor(text, speed, styles){
        this.text = text;
        this.speed = speed;
        this.styles = styles; // styles are strings of the classes which contain unique styles for the text in this Words object
        
    }

    
}